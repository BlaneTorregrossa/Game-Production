using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuBehaviour : MonoBehaviour
{
    //public MenuElementBehaviour _givenElement;
    //public List<GameObject> ElementObjects;

    //public void AddElementsToList(List<GameObject> elementList, Menu.MenuType menuType, Menu currentMenu)
    //{
    //    for (int i = 0; i < elementList.Count; i++)
    //    {
    //        _givenElement = elementList[i].GetComponent<MenuElementBehaviour>();

    //        if (CheckElement(_givenElement, MenuElement.ElementType.TEXT) == true
    //            && menuType == Menu.MenuType.CHARACTERCUSTOMIZATIONMENU)
    //        {
    //            currentMenu.MenuElements.Add(_givenElement.CurrentElement);
    //        }

    //        else if (CheckElement(_givenElement, MenuElement.ElementType.BUTTON) == true
    //            && menuType == Menu.MenuType.STANDARD
    //            || CheckElement(_givenElement, MenuElement.ElementType.BUTTON) == true
    //            && menuType == Menu.MenuType.PAUSEMENU)
    //        {
    //            currentMenu.MenuElements.Add(_givenElement.CurrentElement);
    //        }
    //    }

    //}
    //public bool CheckElement(MenuElementBehaviour Element, MenuElement.ElementType CurrentElement)
    //{
    //    bool returnVariable = false;

    //    if (Element.CurrentElement.Type != CurrentElement)
    //        Debug.Log("Wrong Element!");
    //    else if (Element.CurrentElement.Type == CurrentElement)
    //        returnVariable = true;

    //    return returnVariable;
    //}

    
}
